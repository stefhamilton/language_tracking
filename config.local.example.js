// Copy this file to config.local.js (which is gitignored) and fill in your
// Apps Script Web App URL. app.js falls back to this when localStorage is
// empty, so the app auto-connects on any browser/device without needing you
// to re-paste the URL into Settings every time.
window.HILIGAYNON_SCRIPT_URL = 'https://script.google.com/macros/s/XXXX/exec';
